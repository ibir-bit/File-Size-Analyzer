version: 1

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused

run:
  timeout: 5m
  tests: true
  modules-download-mode: readonly
  skip-dirs:
    - vendor
  go: "1.24"